---
- hosts: all
  become: yes
  roles:
    - role: geerlingguy.docker
      vars:
        docker_users: "{{ lookup('env','USER') }}"
      tags:
        - molecule-notest
    - role: huxoll.azure-cli
